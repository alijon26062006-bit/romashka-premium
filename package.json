{
  "name": "romashka-premium-flower-shop",
  "version": "3.0.0",
  "private": true,
  "description": "Premium flower shop catalog with WhatsApp ordering and admin panel",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node test/bot.test.js && node test/order.test.js && node test/seo.test.js"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "express": "^5.1.0",
    "multer": "^2.0.2"
  }
}
